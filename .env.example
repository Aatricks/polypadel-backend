SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/polypadel
SPRING_DATASOURCE_USERNAME=polypadel
SPRING_DATASOURCE_PASSWORD=changeme

# JWT secret must be base64-encoded (256-bit recommended)
JWT_SECRET=REPLACE_WITH_BASE64_256BIT
JWT_EXP_HOURS=24

# Enable dev-only test endpoints
APP_TEST_ENABLED=false

# CORS allowed origins e.g. http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:5173
# PolyPadel Backend – Environment Example

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/polypadel
SPRING_DATASOURCE_USERNAME=polypadel
SPRING_DATASOURCE_PASSWORD=changeme

# Base64-encoded 256-bit secret (e.g., generated via `openssl rand -base64 32`)
JWT_SECRET=REPLACE_ME_BASE64_256BIT
JWT_EXP_HOURS=24

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Server
SERVER_PORT=8080
